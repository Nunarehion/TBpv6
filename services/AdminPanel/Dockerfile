# Используем легковесный образ Node.js
FROM node:18-alpine

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем только package.json и package-lock.json
COPY package*.json ./

# Устанавливаем все зависимости, необходимые для продакшена.
RUN npm install --omit=dev

# Копируем собранный проект.
COPY build ./

# Открываем порт, который использует ваше приложение
EXPOSE 3000

# Запускаем приложение, принудительно устанавливая переменные окружения HOST и PORT.
# Это гарантирует, что приложение будет слушать на всех сетевых интерфейсах.
CMD ["sh", "-c", "PORT=3000 HOST=0.0.0.0 node index.js"]
